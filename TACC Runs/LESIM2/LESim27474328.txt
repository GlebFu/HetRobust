TACC: Starting up job 7474328
TACC: Setting up parallel environment for MVAPICH2+mpispawn.
TACC: Starting parallel tasks...
Warning message:
Warning message:
Warning message:
Warning message:
Warning message:
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
Warning message:
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
Warning message:
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
Warning message:
Warning message:
Warning message:
Warning message:
Warning message:
Warning message:
Warning message:
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
Warning message:
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
Warning message:
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
Warning message:
Warning message:
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
Warning message:
Warning message:
Warning message:
Warning message:
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
Warning message:
Warning message:
Warning message:
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
Warning message:
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
Warning message:
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
Warning message:
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
Warning message:
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
Warning message:
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
Warning message:
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
Warning message:
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
Warning message:
Warning message:
Warning message:
Warning message:
Warning message:
Warning message:
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
Warning message:
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
Warning message:
Warning message:
Warning message:
Warning message:
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
Warning message:
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
Warning message:
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
Warning message:
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
Warning message:
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
Warning message:
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 

R version 3.0.3 (2014-03-06) -- "Warm Puppy"
Copyright (C) 2014 The R Foundation for Statistical Computing
Platform: x86_64-unknown-linux-gnu (64-bit)

R is free software and comes with ABSOLUTELY NO WARRANTY.
You are welcome to redistribute it under certain conditions.
Type 'license()' or 'licence()' for distribution details.

  Natural language support but running in an English locale

R is a collaborative project with many contributors.
Type 'contributors()' for more information and
'citation()' on how to cite R or R packages in publications.

Type 'demo()' for some demos, 'help()' for on-line help, or
'help.start()' for an HTML browser interface to help.
Type 'q()' to quit R.

Warning message:
Warning message:
Warning message:
Warning message:
Warning message:
Warning message:
Warning message:
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
Warning message:
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
Warning message:
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
Warning message:
Warning message:
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
Warning message:
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
Warning message:
Warning message:
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
Warning message:
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
Warning message:
'.find.package' is deprecated.
Use 'find.package' instead.
See help("Deprecated") 
Loading required package: utils
> #-----------------------------
> # Data-generating model
> #-----------------------------
> 
> Long_dgm <- function(n = 25, B = c(1, 1, 1, 1, 0, 0), Estruct = "E0", whichX = c(T, T ,T ,T ,T ,F), Edist = "En") {
+   
+   # Distributions used in generating data
+   b1 <- runif(n, 0, 1)
+   b2 <- rnorm(n, 0, 1)
+   b3 <- rchisq(n, 1)
+   b4 <- rnorm(n, 0, 1)
+   b5 <- runif(n, 0, 1)
+   
+   # Four independant variables based on distributions
+   x0 <- 1
+   x1 <- 1 + b1
+   x2 <- 3 * b1 + .6 * b2
+   x3 <- 2 * b1 + .6 * b3
+   x4 <- .1 * x1 + .9 * x3 - .8 * b4 + 4 * b5
+   x2[x2 < -2.5] <- -2.5
+   x4[x4 < -2.5] <- -2.5
+   
+   # One dummy variable created by splitting x2
+   xD <- ifelse(x2 > 1.6, 1, 0)
+   
+   # X matrix
+   X <- cbind(x0, x1, x2, x3, x4)
+   
+   # Three types of homoscedastistic error distributions:
+   Edist <- switch(Edist,
+                   En = rnorm(n, 0, 1),
+                   Ech = (rchisq(n, 5) - 5) / sqrt(10),
+                   Et = rt(n, 5))
+   
+   # Seven types of error structures
+   error <- switch(Estruct,
+                   E0 = Edist,
+                   E1 = sqrt(x1) * Edist,
+                   E2 = sqrt(x3 + 1.6) * Edist,
+                   E3 = sqrt(x3) * sqrt(x4 + 2.5) * Edist,
+                   E4 = sqrt(x1) * sqrt(x2 + 2.5) * sqrt(x3) * Edist,
+                   E5 = ifelse(xD == 1, 1.5 * Edist, Edist),
+                   E6 = ifelse(xD == 1, 4 * Edist, Edist)
+   )
+   
+   # Generate DV
+   Y <- as.vector(X %*% B) + error
+   
+   estimate_model(Y, X, B, whichX)
+ 
+ }
> 
> #-----------------------------
> # Run Long & Ervin Simulation
> #-----------------------------
> 
> set.seed(20160808)
> 
> design <- list(n = c(25, 50, 100, 250, 500),
+                B = "1 1 1 1 0 ",
+                whichX = "T T T T T ",
+                Estruct = c("E0", "E1", "E2", "E3", "E4", "E5", "E6"),
+                Edist = c("En", "Ech", "Et"),
+                HC = "HC0 HC1 HC2 HC3 HC4 HC4m HC5",
+                alpha_string = ".010 .050 .100",
+                power = T)
> 
> 
> 
> params <- expand.grid(design, stringsAsFactors = F)
> 
> params$iterations <- 20000
> params$seed <- round(runif(nrow(params)) * 2^30)
> 
> source_obj <- ls()
> 
> library(Rmpi)
> library(snow)
> library(foreach)
> library(iterators)
> library(doSNOW)
> library(plyr)
> 
> cluster <- getMPIcluster()
> registerDoSNOW(cluster)
> 
> clusterExport(cluster, source_obj)
> 
> clusterEvalQ(cluster, source("t-test-and-simulation-functions.R"))
[[1]]
[[1]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[1]]$visible
[1] FALSE


[[2]]
[[2]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[2]]$visible
[1] FALSE


[[3]]
[[3]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[3]]$visible
[1] FALSE


[[4]]
[[4]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[4]]$visible
[1] FALSE


[[5]]
[[5]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[5]]$visible
[1] FALSE


[[6]]
[[6]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[6]]$visible
[1] FALSE


[[7]]
[[7]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[7]]$visible
[1] FALSE


[[8]]
[[8]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[8]]$visible
[1] FALSE


[[9]]
[[9]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[9]]$visible
[1] FALSE


[[10]]
[[10]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[10]]$visible
[1] FALSE


[[11]]
[[11]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[11]]$visible
[1] FALSE


[[12]]
[[12]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[12]]$visible
[1] FALSE


[[13]]
[[13]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[13]]$visible
[1] FALSE


[[14]]
[[14]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[14]]$visible
[1] FALSE


[[15]]
[[15]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[15]]$visible
[1] FALSE


[[16]]
[[16]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[16]]$visible
[1] FALSE


[[17]]
[[17]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[17]]$visible
[1] FALSE


[[18]]
[[18]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[18]]$visible
[1] FALSE


[[19]]
[[19]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[19]]$visible
[1] FALSE


[[20]]
[[20]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[20]]$visible
[1] FALSE


[[21]]
[[21]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[21]]$visible
[1] FALSE


[[22]]
[[22]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[22]]$visible
[1] FALSE


[[23]]
[[23]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[23]]$visible
[1] FALSE


[[24]]
[[24]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[24]]$visible
[1] FALSE


[[25]]
[[25]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[25]]$visible
[1] FALSE


[[26]]
[[26]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[26]]$visible
[1] FALSE


[[27]]
[[27]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[27]]$visible
[1] FALSE


[[28]]
[[28]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[28]]$visible
[1] FALSE


[[29]]
[[29]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[29]]$visible
[1] FALSE


[[30]]
[[30]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[30]]$visible
[1] FALSE


[[31]]
[[31]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[31]]$visible
[1] FALSE


[[32]]
[[32]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[32]]$visible
[1] FALSE


[[33]]
[[33]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[33]]$visible
[1] FALSE


[[34]]
[[34]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[34]]$visible
[1] FALSE


[[35]]
[[35]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[35]]$visible
[1] FALSE


[[36]]
[[36]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[36]]$visible
[1] FALSE


[[37]]
[[37]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[37]]$visible
[1] FALSE


[[38]]
[[38]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[38]]$visible
[1] FALSE


[[39]]
[[39]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[39]]$visible
[1] FALSE


[[40]]
[[40]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[40]]$visible
[1] FALSE


[[41]]
[[41]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[41]]$visible
[1] FALSE


[[42]]
[[42]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[42]]$visible
[1] FALSE


[[43]]
[[43]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[43]]$visible
[1] FALSE


[[44]]
[[44]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[44]]$visible
[1] FALSE


[[45]]
[[45]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[45]]$visible
[1] FALSE


[[46]]
[[46]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[46]]$visible
[1] FALSE


[[47]]
[[47]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[47]]$visible
[1] FALSE


[[48]]
[[48]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[48]]$visible
[1] FALSE


[[49]]
[[49]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[49]]$visible
[1] FALSE


[[50]]
[[50]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[50]]$visible
[1] FALSE


[[51]]
[[51]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[51]]$visible
[1] FALSE


[[52]]
[[52]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[52]]$visible
[1] FALSE


[[53]]
[[53]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[53]]$visible
[1] FALSE


[[54]]
[[54]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[54]]$visible
[1] FALSE


[[55]]
[[55]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[55]]$visible
[1] FALSE


[[56]]
[[56]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[56]]$visible
[1] FALSE


[[57]]
[[57]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[57]]$visible
[1] FALSE


[[58]]
[[58]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[58]]$visible
[1] FALSE


[[59]]
[[59]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[59]]$visible
[1] FALSE


[[60]]
[[60]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[60]]$visible
[1] FALSE


[[61]]
[[61]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[61]]$visible
[1] FALSE


[[62]]
[[62]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[62]]$visible
[1] FALSE


[[63]]
[[63]]$value
function (dgm, iterations, n, B, whichX, HC, alpha_string, power = FALSE, 
    seed = NULL, ...) 
{
    require(reshape2)
    require(plyr)
    if (!requireNamespace("plyr", quietly = TRUE)) {
        stop("The plyr package must be installed")
    }
    B <- as.numeric(unlist(strsplit(B, " ")))
    whichX <- as.logical(unlist(strsplit(whichX, " ")))
    HC <- as.character(unlist(strsplit(HC, " ")))
    alphas <- as.numeric(unlist(strsplit(alpha_string, " ")))
    names(alphas) <- paste0("p", as.character(unlist(strsplit(alpha_string, 
        " "))))
    if (!is.null(seed)) 
        set.seed(seed)
    reps <- plyr::rdply(iterations, {
        model <- dgm(n = n, B = B, whichX = whichX, ...)
        results <- plyr::ldply(HC, estimate, model = model, alphas = alphas, 
            power = power)
        oresults <- OLS(model, power = power)
        merge(results, oresults, all = T)
    })
    repsL <- melt(reps[, -1], measure.vars = names(reps[5:ncol(reps)]), 
        variable.name = "test", value.name = "pval")
    ddply(repsL, .(HC, coef, criterion, test), summarize, percent_NA = mean(is.na(pval)), 
        p.005 = reject_rates(pval, 0.005), p.010 = reject_rates(pval, 
            0.01), p.050 = reject_rates(pval, 0.05), p.100 = reject_rates(pval, 
            0.1))
}

[[63]]$visible
[1] FALSE


> clusterEvalQ(cluster, library(plyr))
[[1]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[2]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[3]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[4]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[5]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[6]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[7]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[8]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[9]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[10]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[11]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[12]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[13]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[14]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[15]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[16]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[17]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[18]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[19]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[20]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[21]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[22]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[23]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[24]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[25]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[26]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[27]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[28]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[29]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[30]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[31]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[32]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[33]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[34]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[35]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[36]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[37]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[38]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[39]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[40]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[41]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[42]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[43]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[44]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[45]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[46]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[47]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[48]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[49]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[50]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[51]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[52]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[53]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[54]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[55]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[56]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[57]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[58]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[59]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[60]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[61]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[62]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

[[63]]
 [1] "plyr"      "stats"     "graphics"  "grDevices" "datasets"  "snow"     
 [7] "utils"     "Rmpi"      "methods"   "base"     

> clusterEvalQ(cluster, library(reshape2))
[[1]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[2]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[3]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[4]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[5]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[6]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[7]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[8]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[9]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[10]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[11]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[12]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[13]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[14]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[15]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[16]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[17]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[18]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[19]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[20]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[21]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[22]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[23]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[24]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[25]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[26]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[27]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[28]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[29]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[30]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[31]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[32]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[33]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[34]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[35]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[36]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[37]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[38]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[39]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[40]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[41]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[42]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[43]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[44]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[45]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[46]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[47]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[48]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[49]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[50]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[51]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[52]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[53]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[54]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[55]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[56]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[57]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[58]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[59]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[60]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[61]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[62]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

[[63]]
 [1] "reshape2"  "plyr"      "stats"     "graphics"  "grDevices" "datasets" 
 [7] "snow"      "utils"     "Rmpi"      "methods"   "base"     

> 
> 
> system.time(results <- plyr::mdply(params, .fun = runSim, 
+                                    dgm = Long_dgm,
+                                    .parallel = T))
[c447-601.stampede.tacc.utexas.edu:mpispawn_3][readline] Unexpected End-Of-File on file descriptor 18. MPI process died?
[c447-601.stampede.tacc.utexas.edu:mpispawn_3][mtpmi_processops] Error while reading PMI socket. MPI process died?
[c447-601.stampede.tacc.utexas.edu:mpispawn_3][child_handler] MPI process (rank: 62, pid: 45555) terminated with signal 9 -> abort job
[c419-402.stampede.tacc.utexas.edu:mpirun_rsh][process_mpispawn_connection] mpispawn_3 from node c447-601 aborted: Error while reading a PMI socket (4)
[c419-402.stampede.tacc.utexas.edu:mpispawn_0][read_size] Unexpected End-Of-File on file descriptor 24. MPI process died?
[c419-402.stampede.tacc.utexas.edu:mpispawn_0][read_size] Unexpected End-Of-File on file descriptor 24. MPI process died?
[c419-402.stampede.tacc.utexas.edu:mpispawn_0][handle_mt_peer] Error while reading PMI socket. MPI process died?
[c447-101.stampede.tacc.utexas.edu:mpispawn_2][read_size] Unexpected End-Of-File on file descriptor 21. MPI process died?
[c447-101.stampede.tacc.utexas.edu:mpispawn_2][read_size] Unexpected End-Of-File on file descriptor 21. MPI process died?
[c447-101.stampede.tacc.utexas.edu:mpispawn_2][handle_mt_peer] Error while reading PMI socket. MPI process died?
[c430-902.stampede.tacc.utexas.edu:mpispawn_1][read_size] Unexpected End-Of-File on file descriptor 21. MPI process died?
[c430-902.stampede.tacc.utexas.edu:mpispawn_1][read_size] Unexpected End-Of-File on file descriptor 21. MPI process died?
[c430-902.stampede.tacc.utexas.edu:mpispawn_1][handle_mt_peer] Error while reading PMI socket. MPI process died?
TACC: MPI job exited with code: 1
 
TACC: Shutdown complete. Exiting.
